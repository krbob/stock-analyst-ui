# In dev mode, Vite proxies /api/* to the backend (see vite.config.ts).
# Set this only if you need to override the default proxy behavior.
# VITE_API_URL=/api

# Set to false in .env.local to hide the local development attribution footer.
# VITE_SHOW_CHART_ATTRIBUTION=false
